## Agent skills

### Issue tracker

Local markdown files under `.scratch/<feature-slug>/` — this repo has no GitHub/GitLab remote. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical roles, recorded as a `Status:` line on each local issue file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
